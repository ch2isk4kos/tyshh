<h1>Post Up Index</h1>

<div class="#">
    <% @posts.each do |post|%>
        <h2><%= post.title %></h2>
        <p><%= post.body %></p>
    <%end %>
</div>

<div class="#">
    <%= button_to "Create Take", new_post_path %>
</div>
